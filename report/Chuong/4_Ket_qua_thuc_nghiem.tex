\documentclass[../DoAn.tex]{subfiles}
\begin{document}

Tiếp nối Chương 3, nơi các nền tảng công nghệ và cơ sở lý thuyết đã được phân tích, Chương 4 tập trung hiện thực hóa các lựa chọn đó thành kiến trúc phần mềm, thiết kế dữ liệu, giao diện và quy trình triển khai cụ thể của hệ thống marketplace đa nhà bán hàng. Nội dung chương lần lượt trình bày về: thiết kế kiến trúc và biểu đồ gói của hệ thống trong phần \ref{section:4.1}; thiết kế chi tiết giao diện, mô-đun nghiệp vụ và cơ sở dữ liệu trong phần \ref{section:4.2}; quá trình xây dựng ứng dụng cùng các công cụ, thư viện và kết quả đạt được trong phần \ref{section:4.3}; kiểm thử hệ thống trong phần \ref{section:4.4}; và sau cùng là mô hình triển khai thực tế của đồ án trong phần \ref{section:4.5}.

% ─── SECTION 1: THIẾT KẾ KIẾN TRÚC ─────────────────────────────────────────
\section{Thiết kế kiến trúc}
\label{section:4.1}

\subsection{Lựa chọn kiến trúc phần mềm}
Đồ án lựa chọn kiến trúc web nhiều tầng theo định hướng MERN Stack với hai nền tảng độc lập: ứng dụng giao diện người dùng (frontend) viết bằng React 19 và máy chủ xử lý nghiệp vụ (backend) viết bằng Node.js/Express 5. Hai nền tảng giao tiếp qua giao thức REST trên HTTPS; kênh chat thời gian thực được tách riêng thông qua Socket.IO (WebSocket), còn luồng thông báo một chiều sử dụng Server-Sent Events (SSE).

\textbf{Kiến trúc phân lớp phía máy chủ.} Thay vì phát triển theo hướng vi dịch vụ ngay từ đầu, backend được hiện thực dưới dạng một máy chủ Express duy nhất nhưng phân tách nội bộ thành bốn tầng rõ ràng:

(i) \textit{Tầng định tuyến} (\texttt{routes/}): tiếp nhận yêu cầu HTTP, áp dụng middleware xác thực JWT và giới hạn tần suất, rồi chuyển tiếp đến Controller tương ứng.

(ii) \textit{Tầng điều phối} (\texttt{controllers/}): phân giải tham số từ đối tượng \texttt{Request}, gọi Service và trả phản hồi JSON chuẩn hóa về client.

(iii) \textit{Tầng nghiệp vụ} (\texttt{services/}): chứa toàn bộ logic xử lý, bao gồm tính toán giá, quản lý tồn kho, phiên xác thực và giao tiếp với các dịch vụ bên ngoài (Stripe, VNPay, Cloudflare R2).

(iv) \textit{Tầng dữ liệu} (\texttt{models/}): định nghĩa schema Mongoose và trừu tượng hóa tương tác với MongoDB.

Nguyên tắc thiết kế quan trọng là các tầng chỉ phụ thuộc theo một chiều từ trên xuống dưới: Route phụ thuộc Controller, Controller phụ thuộc Service, Service phụ thuộc Model. Không tầng nào phụ thuộc ngược hoặc bỏ qua tầng kế tiếp.

\textbf{Kiến trúc thành phần phía giao diện.} Frontend áp dụng kiến trúc thành phần (Component-Based Architecture) đặc trưng của React, được tổ chức theo mô hình phân cấp bốn tầng: Pages (màn hình chính), Components (thành phần giao diện tái sử dụng), Context và Hooks (quản lý trạng thái và logic nghiệp vụ phía client), Utils (hàm tiện ích định dạng).

\textbf{Lý do không chọn Microservices.} Dù kiến trúc Microservices mang lại khả năng mở rộng độc lập theo từng dịch vụ, phạm vi đồ án là hệ thống thương mại điện tử một cụm máy chủ với lưu lượng vừa phải. Kiến trúc nguyên khối phân lớp giảm đáng kể chi phí vận hành, đơn giản hóa giao dịch nguyên tử giữa các nghiệp vụ (đặt hàng và giữ kho), đồng thời phù hợp với quy mô phát triển của một nhóm nhỏ. Cách phân chia mã nguồn hiện tại vẫn đủ tách biệt để có thể tách thành vi dịch vụ trong giai đoạn sau nếu cần thiết.

\subsection{Kiến trúc triển khai tổng thể}
\label{section:4.1.2}

Hình \ref{fig:architecture_overview} mô tả kiến trúc triển khai tổng thể của hệ thống dưới dạng sơ đồ triển khai (deployment diagram). Phía khách hàng truy cập ứng dụng mua sắm xây dựng bằng React + Vite; phía đối tác bán hàng sử dụng một ứng dụng React độc lập để quản trị sản phẩm, đơn hàng, voucher và thống kê. Cả hai giao diện cùng giao tiếp với backend Express thông qua REST API, trong đó luồng chat thời gian thực sử dụng Socket.IO còn thông báo trên dashboard vendor sử dụng Server-Sent Events (SSE).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth,height=0.78\textheight,keepaspectratio]{images/generated/architecture_overview.png}
    \caption{Kiến trúc triển khai tổng thể của hệ thống marketplace}
    \label{fig:architecture_overview}
\end{figure}

\subsection{Biểu đồ gói}
\label{section:4.1.3}

Để làm rõ cấu trúc nội bộ của từng nền tảng, phần này trình bày biểu đồ gói UML (package diagram) cho cả backend và frontend, thể hiện sự phụ thuộc một chiều giữa các gói theo từng tầng.

Biểu đồ gói phía máy chủ trong Hình \ref{fig:be_package} trình bày bảy gói chính của backend với phụ thuộc một chiều từ ngoài vào trong. Gói \texttt{routes} phụ thuộc vào \texttt{middleware} và \texttt{controllers}; \texttt{controllers} phụ thuộc \texttt{services} và \texttt{utils}; \texttt{services} phụ thuộc \texttt{models} và \texttt{config}. Hai gói ngoại vi là \texttt{models} (kết nối MongoDB) và \texttt{config} (cấu hình Redis, Cloudflare R2) không phụ thuộc bất kỳ gói nghiệp vụ nào.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.78\textheight,keepaspectratio]{images/generated/backend_package_diagram.png}
    \caption{Biểu đồ gói phía máy chủ}
    \label{fig:be_package}
\end{figure}

Biểu đồ gói phía giao diện trong Hình \ref{fig:fe_package} cho thấy gói \texttt{pages} sử dụng \texttt{components} để dựng giao diện, tiêu thụ trạng thái từ \texttt{context} và gọi logic nghiệp vụ thông qua \texttt{hooks}. Gói \texttt{hooks} là cầu nối giữa tầng hiển thị và tầng gọi API: hook đọc/ghi trạng thái trong \texttt{context} và gửi yêu cầu qua gói \texttt{api}. Gói \texttt{utils} cung cấp các hàm tiện ích thuần túy và không phụ thuộc bất kỳ gói nào khác.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/generated/frontend_package_diagram.png}
    \caption{Biểu đồ gói phía giao diện (Frontend)}
    \label{fig:fe_package}
\end{figure}

\subsection{Thiết kế chi tiết gói}
\label{section:4.1.4}

Phần này trình bày biểu đồ lớp thiết kế cho từng gói hoặc nhóm gói liên quan, thể hiện tên các lớp và quan hệ giữa chúng. Các gói ngoại vi được tô nền xám để phân biệt với gói đang được mô tả.

\textbf{Nhóm gói \texttt{routes} và \texttt{middleware}.} Hình \ref{fig:routes_middleware_class} mô tả quan hệ phụ thuộc giữa các lớp định tuyến và lớp xác thực trung gian. Mỗi lớp Route (ProductRoute, OrderRoute, VoucherRoute, AuthRoute, UserRoute) phụ thuộc vào \texttt{auth} để xác minh JWT trước khi chuyển tiếp yêu cầu. Các route yêu cầu quyền nhà bán hàng bổ sung phụ thuộc thêm vào \texttt{vendorAuth}, lớp này đến lượt mình lại phụ thuộc vào \texttt{auth}. Từng lớp Route phụ thuộc vào Controller tương ứng để điều phối xử lý nghiệp vụ.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.72\textheight,keepaspectratio]{images/generated/routes_middleware_class.png}
    \caption{Biểu đồ lớp nhóm gói \texttt{routes} và \texttt{middleware}}
    \label{fig:routes_middleware_class}
\end{figure}

\textbf{Gói \texttt{controllers}.} Hình \ref{fig:controllers_class} mô tả quan hệ phụ thuộc giữa các lớp Controller và Service tương ứng. Mỗi Controller (ProductController, OrderController, VoucherController, AuthController, UserController, ChatController) giữ vai trò điều phối mỏng: phân giải tham số đầu vào từ \texttt{req.body}/\texttt{req.params}/\texttt{req.query}, gọi đúng hàm Service, xử lý ngoại lệ và trả phản hồi JSON chuẩn hóa. Controller không chứa logic nghiệp vụ hay truy vấn cơ sở dữ liệu trực tiếp.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.72\textheight,keepaspectratio]{images/generated/controllers_class.png}
    \caption{Biểu đồ lớp gói \texttt{controllers}}
    \label{fig:controllers_class}
\end{figure}

\textbf{Gói \texttt{services}.} Hình \ref{fig:svc_class} mô tả quan hệ giữa các Service chủ đạo. Đây là gói trung tâm của kiến trúc — mỗi miền nghiệp vụ có một lớp Service tương ứng. \texttt{OrderService} giữ vai trò điều phối trung tâm: phụ thuộc vào \texttt{VoucherService} để tính bảng giá nhiều lớp, \texttt{StockReservationService} để giữ kho nguyên tử và \texttt{NotificationService} để đẩy sự kiện thời gian thực tới nhà bán hàng. \texttt{SearchService} phụ thuộc vào \texttt{InteractionService} để cá nhân hóa kết quả tìm kiếm theo hành vi người dùng.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{images/generated/service_class_diagram.png}
    \caption{Biểu đồ lớp gói \texttt{services}}
    \label{fig:svc_class}
\end{figure}

\textbf{Nhóm gói \texttt{context} và \texttt{hooks} (Frontend).} Hình \ref{fig:context_hooks_class} mô tả quan hệ giữa các lớp quản lý trạng thái và logic nghiệp vụ phía giao diện. \texttt{ShopContext} lưu trữ tập trung các trạng thái toàn cục gồm danh sách sản phẩm, giỏ hàng, thông tin người dùng và token xác thực. Các hook (usePlaceOrderCheckout, useCart, useAuth, useSearch) có quan hệ kết hợp với \texttt{ShopContext} để đọc và cập nhật trạng thái, đồng thời phụ thuộc vào các module \texttt{api} tương ứng để gửi yêu cầu tới backend. Mũi tên liền thể hiện quan hệ kết hợp; mũi tên đứt thể hiện quan hệ phụ thuộc.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.72\textheight,keepaspectratio]{images/generated/context_hooks_class.png}
    \caption{Biểu đồ lớp nhóm gói \texttt{context} và \texttt{hooks}}
    \label{fig:context_hooks_class}
\end{figure}

% ─── SECTION 2: THIẾT KẾ CHI TIẾT ──────────────────────────────────────────
\section{Thiết kế chi tiết}
\label{section:4.2}

\subsection{Thiết kế giao diện}
Hệ thống giao diện được phát triển theo mô hình SPA với hai ứng dụng độc lập dùng chung định hướng thiết kế. Ứng dụng \texttt{frontend} phục vụ khách hàng cuối gồm các nhóm màn hình: trang chủ, danh sách sản phẩm, trang chi tiết sản phẩm, giỏ hàng, đặt hàng, theo dõi đơn, hồ sơ cá nhân, chat và thông báo. Ứng dụng vendor dashboard phục vụ đối tác bán hàng gồm các màn hình quản lý sản phẩm, đơn hàng, voucher, thống kê và chat.

\textbf{Đặc tả màn hình.} Giao diện được thiết kế theo phương pháp \textit{mobile-first responsive} với ba ngưỡng kích thước: bố cục một cột trên màn hình từ 360px (mobile), hai cột trên máy tính bảng từ 768px (tablet) và nhiều cột linh hoạt trên màn hình máy tính từ 1280px trở lên (desktop). Hệ thống hỗ trợ độ sâu màu 24-bit (true color, hơn 16 triệu màu). Bố cục sử dụng Tailwind CSS 3.4 với lưới 12 cột. Hệ thống phông chữ sử dụng \textit{Inter} làm phông chính với ba cỡ chữ chuẩn: 14px cho văn bản thông thường, 16px cho nhãn và nút bấm, và 20--24px cho tiêu đề màn hình.

\textbf{Hệ màu.} Bảng màu được xây dựng từ ba nhóm chính. Màu chủ đạo là cam (\texttt{\#F97316}) dùng cho nút hành động chính, liên kết tương tác và các phần tử kêu gọi hành động. Màu phụ là xám trung tính (\texttt{\#6B7280}) dùng cho văn bản thứ yếu, nhãn form và đường phân cách. Màu nền vùng nội dung là trắng (\texttt{\#FFFFFF}) và nền trang là xám nhạt (\texttt{\#F9FAFB}). Màu trạng thái gồm xanh lá (\texttt{\#22C55E}) cho thành công, đỏ (\texttt{\#EF4444}) cho lỗi và vàng (\texttt{\#F59E0B}) cho cảnh báo.

\textbf{Chuẩn hóa nút bấm.} Bảng \ref{table:ui_button} trình bày bốn kiểu nút được thống nhất trong toàn hệ thống. Mọi nút đều có chiều cao 40px, padding ngang 16px, bo góc 6px và chuyển động hover 150ms.

\begin{table}[H]
    \centering
    \caption{Chuẩn hóa các kiểu nút bấm}
    \label{table:ui_button}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{2.4cm}|>{\raggedright\arraybackslash}p{2.0cm}|>{\raggedright\arraybackslash}p{6.0cm}|}
        \hline
        \textbf{Kiểu nút} & \textbf{Màu nền} & \textbf{Màu chữ} & \textbf{Ngữ cảnh sử dụng} \\ \hline
        Primary & \texttt{\#F97316} (cam) & Trắng & Hành động chính: Thêm vào giỏ, Đặt hàng, Lưu. \\ \hline
        Secondary & Trắng, viền \texttt{\#D1D5DB} & \texttt{\#374151} & Hành động phụ: Quay lại, Hủy, Xem thêm. \\ \hline
        Danger & \texttt{\#EF4444} (đỏ) & Trắng & Thao tác nguy hiểm: Xóa sản phẩm, Hủy đơn. \\ \hline
        Disabled & \texttt{\#E5E7EB} (xám nhạt) & \texttt{\#9CA3AF} & Nút bị vô hiệu hóa khi điều kiện chưa thỏa. \\ \hline
    \end{tabular}
\end{table}

\textbf{Chuẩn hóa điều khiển nhập liệu.} Bảng \ref{table:ui_control} liệt kê các thành phần điều khiển được dùng thống nhất. Mọi ô nhập liệu đều có chiều cao 40px, viền \texttt{\#D1D5DB}, bo góc 6px; khi được chọn (focus) viền chuyển sang cam \texttt{\#F97316}; khi có lỗi validation viền chuyển đỏ \texttt{\#EF4444} kèm thông báo lỗi nhỏ bên dưới.

\begin{table}[H]
    \centering
    \caption{Chuẩn hóa các thành phần điều khiển nhập liệu}
    \label{table:ui_control}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{2.8cm}|>{\raggedright\arraybackslash}p{10.7cm}|}
        \hline
        \textbf{Thành phần} & \textbf{Đặc tả} \\ \hline
        Text input & Chiều cao 40px, nền trắng, placeholder màu \texttt{\#9CA3AF}. Focus: viền cam. Lỗi: viền đỏ + thông báo 12px bên dưới. \\ \hline
        Select / Dropdown & Cùng kích thước text input, có icon mũi tên xuống bên phải. Danh sách tùy chọn hiển thị dưới dạng floating menu. \\ \hline
        Checkbox / Radio & Kích thước 16×16px. Trạng thái checked: nền cam, dấu tích trắng. Dùng checkbox cho lựa chọn nhiều, radio cho lựa chọn một. \\ \hline
        File upload & Vùng kéo thả có viền đứt nét, hiển thị icon upload và hướng dẫn; hỗ trợ cả click để chọn file. \\ \hline
        Search input & Text input có icon kính lúp bên trái, hỗ trợ gợi ý tức thì (debounce 300ms). \\ \hline
    \end{tabular}
\end{table}

\textbf{Chuẩn tương tác và phản hồi.} Hệ thống thống nhất ba quy tắc phản hồi người dùng: (i) thông báo kết quả thao tác hiển thị dưới dạng toast ở góc trên bên phải màn hình, tự động biến mất sau 3 giây; (ii) hộp thoại xác nhận thao tác nguy hiểm (xóa, hủy) dùng modal phủ nền mờ toàn màn hình; (iii) trạng thái đang tải dữ liệu hiển thị skeleton placeholder cùng hình dạng với nội dung thực để tránh layout shift.

Sơ đồ điều hướng chính của hai ứng dụng giao diện được minh họa trong Hình \ref{fig:ui_navigation_map}. Người mua đi từ khám phá sản phẩm đến giỏ hàng, đặt hàng và theo dõi đơn; vendor di chuyển chủ yếu giữa danh sách sản phẩm, đơn hàng, voucher, thống kê và chat.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.76\textheight,keepaspectratio]{images/generated/ui_navigation_map.png}
    \caption{Sơ đồ điều hướng giao diện chính của hệ thống}
    \label{fig:ui_navigation_map}
\end{figure}

\textbf{Mockup thiết kế giao diện.} Hình \ref{fig:mockup_home}, \ref{fig:mockup_checkout} và \ref{fig:mockup_vendor} lần lượt minh họa thiết kế wireframe cho ba màn hình quan trọng nhất của hệ thống.

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/mockup/mockup_home.png}{
        \includegraphics[width=0.92\textwidth,height=0.55\textheight,keepaspectratio]{Chuong/images/mockup/mockup_home.png}
    }{
        \fbox{\rule{0pt}{7cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Mockup giao diện trang chủ ứng dụng mua sắm}
    \label{fig:mockup_home}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/mockup/mockup_checkout.png}{
        \includegraphics[width=0.92\textwidth,height=0.55\textheight,keepaspectratio]{Chuong/images/mockup/mockup_checkout.png}
    }{
        \fbox{\rule{0pt}{7cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Mockup giao diện trang đặt hàng và thanh toán}
    \label{fig:mockup_checkout}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/mockup/mockup_vendor.png}{
        \includegraphics[width=0.92\textwidth,height=0.55\textheight,keepaspectratio]{Chuong/images/mockup/mockup_vendor.png}
    }{
        \fbox{\rule{0pt}{7cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Mockup giao diện bảng điều khiển nhà bán hàng}
    \label{fig:mockup_vendor}
\end{figure}

\subsection{Thiết kế lớp}
\label{section:4.2.2}

Phần này trình bày thiết kế chi tiết thuộc tính và phương thức cho bốn lớp chủ đạo của hệ thống, gồm hai lớp thực thể dữ liệu (\texttt{Order}, \texttt{Product}) và hai lớp dịch vụ nghiệp vụ (\texttt{OrderService}, \texttt{VoucherService}). Ký hiệu \texttt{-} chỉ thuộc tính/phương thức private, \texttt{+} chỉ public.

\textbf{Lớp thực thể \texttt{Order} và \texttt{Product}.} Hình \ref{fig:model_class_detail} mô tả thiết kế chi tiết hai lớp thực thể. Lớp \texttt{Order} bổ sung ba trường đặc thù so với đơn hàng thông thường: \texttt{idempotencyKey} chống đặt trùng, \texttt{stockReservedAt} và \texttt{reservation\allowbreak ExpiresAt} để quản lý vòng đời giữ kho; phương thức \texttt{isReservation\allowbreak Expired()} được tác vụ nền gọi định kỳ để phát hiện đơn quá hạn. Lớp \texttt{Product} hỗ trợ mảng \texttt{variants} (hợp thành) lưu tổ hợp SKU với tồn kho độc lập; phương thức \texttt{reserveStock()} thực hiện cập nhật nguyên tử bằng điều kiện \texttt{\$inc} kết hợp kiểm tra số lượng để tránh bán vượt tồn kho đồng thời. Chi tiết về cơ chế giữ kho nguyên tử và khóa \texttt{idempotencyKey} chống trùng lặp giao dịch được phân tích sâu tại Phần~\ref{section:5.1}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth,height=0.76\textheight,keepaspectratio]{images/generated/model_class_detail.png}
    \caption{Thiết kế lớp \texttt{Order} và \texttt{Product}}
    \label{fig:model_class_detail}
\end{figure}

\textbf{Lớp dịch vụ \texttt{OrderService} và \texttt{VoucherService}.} Hình \ref{fig:service_class_detail} mô tả thiết kế chi tiết hai lớp dịch vụ trung tâm. \texttt{OrderService} là lớp điều phối chính: \texttt{createOrder()} kiểm tra idempotency trước, sau đó gọi \texttt{VoucherService.\allowbreak computeOrderPricing()} để tính bảng giá và \texttt{reserveStock()} (private) để giữ kho nguyên tử; \texttt{cancelExpired\allowbreak Orders()} chạy như tác vụ nền định kỳ để hoàn trả tồn kho và voucher khi đơn quá hạn. \texttt{VoucherService} tập trung toàn bộ logic giảm giá: \texttt{computeOrderPricing()} gọi lần lượt ba phương thức private để áp dụng từng lớp voucher theo thứ tự ưu tiên shop → platform → shipping; \texttt{assertVoucher\allowbreak Valid()} ném ngoại lệ sớm nếu voucher hết hạn hoặc hết lượt, tránh xử lý không cần thiết. Thiết kế chi tiết bộ tính toán giá phân tầng của \texttt{VoucherService} và kiến trúc điều phối đơn hàng đa nhà bán hàng được trình bày tại Phần~\ref{section:5.3}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth,height=0.76\textheight,keepaspectratio]{images/generated/service_class_detail.png}
    \caption{Thiết kế lớp \texttt{OrderService} và \texttt{VoucherService}}
    \label{fig:service_class_detail}
\end{figure}

Để minh họa luồng tương tác giữa các lớp trên, biểu đồ tuần tự cho hai use case quan trọng được trình bày trong phần tiếp theo.

\subsection{Thiết kế mô hình nghiệp vụ và tương tác}
\label{section:4.2.3}

\textbf{Các thực thể nghiệp vụ chủ đạo.} Bốn đối tượng nghiệp vụ quan trọng nhất là \texttt{User}, \texttt{Product}, \texttt{Order} và \texttt{Voucher}. \texttt{User} giữ thông tin tài khoản hợp nhất cho cả người mua và người bán, phân biệt bằng trường \texttt{role}, giúp đơn giản hóa luồng xác thực. \texttt{Product} hỗ trợ cấu trúc phân cấp danh mục ba tầng, tập thuộc tính và biến thể SKU với tồn kho riêng từng SKU. \texttt{Order} ngoài các trường giao dịch cơ bản còn bổ sung \texttt{idempotencyKey}, \texttt{stockReservedAt}, \texttt{reservationExpiresAt} và dữ liệu cổng thanh toán để hỗ trợ giữ kho và thanh toán online an toàn. \texttt{Voucher} hỗ trợ ba kiểu \texttt{SHOP}, \texttt{PLATFORM} và \texttt{SHIPPING} với hai kiểu giảm giá phần trăm và số tiền cố định.

Luồng tương tác giữa các thành phần nghiệp vụ không chỉ nằm ở cấu trúc thực thể mà còn ở cơ chế phối hợp giữa các dịch vụ xử lý. Mối quan hệ điều phối giữa các Service lớp đã được trình bày trong Hình \ref{fig:svc_class}. Trong đó \texttt{OrderService} đóng vai trò điều phối trung tâm: khi đặt hàng nó gọi \texttt{VoucherService.computeOrderPricing()} để tính bảng giá nhiều lớp, sử dụng các dịch vụ con trong miền \texttt{order/} để giữ kho nguyên tử và kích hoạt dịch vụ thông báo để đẩy sự kiện thời gian thực tới nhà bán hàng.

\textbf{Biểu đồ tuần tự: Tạo đơn hàng.} Hình \ref{fig:seq_order} mô tả luồng tạo đơn hàng với cơ chế kiểm soát trùng lệnh và giữ kho nguyên tử. Sau khi client gửi yêu cầu kèm mã định danh chống trùng đơn, backend tra cứu đơn cũ trước; nếu chưa tồn tại, hệ thống thực hiện giữ kho bằng phép cập nhật có điều kiện ($\$inc$ kết hợp điều kiện số lượng đủ) để ngăn bán vượt tồn kho đồng thời. Thông tin thời điểm và thời hạn giữ kho được lưu vào bản ghi đơn hàng để tác vụ nền có thể hoàn trả nếu thanh toán quá hạn. Phân tích chi tiết cơ chế này được trình bày tại Phần~\ref{section:5.1}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.78\textheight,keepaspectratio]{images/generated/order_creation_sequence.png}
    \caption{Biểu đồ tuần tự luồng tạo đơn hàng và giữ kho nguyên tử}
    \label{fig:seq_order}
\end{figure}

\textbf{Biểu đồ tuần tự: Xác nhận thanh toán trực tuyến và Tác vụ quét nền.} Hình \ref{fig:seq_payment} mô tả hai kịch bản xác nhận thanh toán trực tuyến (Stripe và VNPay), trong khi Hình \ref{fig:payment_sweeper_sequence} mô tả cơ chế tự động quét dọn và hủy các đơn hàng quá hạn thanh toán. Với Stripe, kết quả thanh toán được truyền về qua webhook; với VNPay, kết quả truyền qua URL callback có chữ ký số. Tác vụ quét nền (reservation sweeper) chạy định kỳ để phát hiện các đơn chưa thanh toán đã vượt quá thời hạn giữ kho, tự động chuyển trạng thái sang đã hủy, hoàn trả tồn kho và khôi phục lượt sử dụng voucher. Kiến trúc xử lý thanh toán và điều phối đơn hàng đa nhà bán hàng được phân tích sâu tại Phần~\ref{section:5.3}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.78\textheight,keepaspectratio]{images/generated/online_payment_confirmation_sequence.png}
    \caption{Biểu đồ tuần tự xác nhận thanh toán trực tuyến}
    \label{fig:seq_payment}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth,keepaspectratio]{images/generated/payment_sweeper_sequence.png}
    \caption{Biểu đồ tuần tự tác vụ nền quét hủy đơn hàng quá hạn thanh toán}
    \label{fig:payment_sweeper_sequence}
\end{figure}

\subsection{Thiết kế cơ sở dữ liệu}
Mô hình dữ liệu chính của hệ thống được thể hiện trong Hình \ref{fig:marketplace_er_diagram}. Dù triển khai trên MongoDB, sơ đồ vẫn được trình bày theo kiểu thực thể liên kết để làm rõ các quan hệ nghiệp vụ cốt lõi giữa người dùng, sản phẩm, đơn hàng, voucher, đánh giá, tương tác và hội thoại.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.98\textwidth,height=0.84\textheight,keepaspectratio]{images/generated/marketplace_er_diagram.png}
    \caption{Mô hình dữ liệu chính của hệ thống marketplace}
    \label{fig:marketplace_er_diagram}
\end{figure}

Lược đồ dữ liệu được thiết kế theo hướng kết hợp giữa tham chiếu và nhúng tài liệu. Các tài liệu có vòng đời độc lập như \texttt{user}, \texttt{product}, \texttt{voucher}, \texttt{review}, \texttt{notification} hay \texttt{conversation} được tách thành các collection riêng. Trong khi đó, các cấu phần gắn chặt với đơn hàng như \texttt{items}, \texttt{appliedVouchers} và thông tin theo vendor được nhúng trực tiếp vào tài liệu \texttt{order} để giảm số lần join logic ở tầng ứng dụng và bảo toàn trạng thái giao dịch theo thời điểm đặt hàng.

Về tối ưu truy vấn, collection \texttt{product} có chỉ mục văn bản trên \texttt{name}, \texttt{brand} và \texttt{tags} để phục vụ tìm kiếm server-side. Collection \texttt{order} có chỉ mục duy nhất trên cặp \texttt{userId} và \texttt{idempotencyKey} để chặn tạo đơn trùng (xem chi tiết cơ chế tại Phần~\ref{section:5.1}). Collection \texttt{userInteraction} có chỉ mục kết hợp \texttt{userId}~--~\texttt{productId} nhằm bảo đảm mỗi người dùng chỉ có một hồ sơ tương tác cho một sản phẩm; cấu trúc này là nền tảng cho hệ thống gợi ý lai được trình bày tại Phần~\ref{section:5.2}.

Cấu trúc các collection quan trọng nhất được trình bày chi tiết từ Bảng \ref{table:schema_user} đến Bảng \ref{table:schema_interaction}.

\begin{table}[H]
    \centering
    \caption{Cấu trúc collection \texttt{users} (Người dùng và nhà bán hàng)}
    \label{table:schema_user}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.0cm}|>{\raggedright\arraybackslash}p{1.7cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{5.3cm}|}
        \hline
        \textbf{Trường dữ liệu} & \textbf{Kiểu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\ \hline
        \texttt{\_id} & ObjectId & Khóa chính & Định danh duy nhất của người dùng. \\ \hline
        \texttt{name} & String & Required & Họ và tên người dùng. \\ \hline
        \texttt{email} & String & Unique, Required & Địa chỉ email dùng để đăng nhập. \\ \hline
        \texttt{password} & String & Optional & Mật khẩu đăng nhập cục bộ, được mã hóa bằng bcrypt khi tồn tại. \\ \hline
        \texttt{role} & String & Default: 'user' & Vai trò: \texttt{'user'} hoặc \texttt{'vendor'}. \\ \hline
        \texttt{avatar} & Object & -- & Đường dẫn ảnh gốc và ảnh thu nhỏ (thumbnail). \\ \hline
        \texttt{cartData} & Object & Default: \{\} & Giỏ hàng lưu tạm của khách hàng. \\ \hline
        \texttt{shopName} & String & -- & Tên gian hàng (chỉ dành cho vendor). \\ \hline
        \texttt{email}\allowbreak\texttt{Verified} & Boolean & Default: false & Trạng thái xác thực email. \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Cấu trúc collection \texttt{products} (Sản phẩm)}
    \label{table:schema_product}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.0cm}|>{\raggedright\arraybackslash}p{1.7cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{5.3cm}|}
        \hline
        \textbf{Trường dữ liệu} & \textbf{Kiểu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\ \hline
        \texttt{\_id} & ObjectId & Khóa chính & Định danh duy nhất của sản phẩm. \\ \hline
        \texttt{name} & String & Required & Tên sản phẩm (chỉ mục text). \\ \hline
        \texttt{price} & Number & Required & Giá bán cơ bản. \\ \hline
        \texttt{stock} & Number & Default: 0 & Số lượng tồn kho cơ bản. \\ \hline
        \texttt{category} & ObjectId & Ref: category & Danh mục cấp 1 của sản phẩm. \\ \hline
        \texttt{vendorId} & ObjectId & Ref: user & Định danh vendor sở hữu sản phẩm. \\ \hline
        \texttt{sold} & Number & Default: 0 & Số lượng sản phẩm đã bán. \\ \hline
        \texttt{rating} & Number & Default: 0 & Điểm đánh giá trung bình (1--5). \\ \hline
        \texttt{attributes} & Array & -- & Tập thuộc tính (tên và danh sách giá trị). \\ \hline
        \texttt{variants} & Array & -- & Biến thể SKU (tổ hợp thuộc tính, giá và tồn kho riêng). \\ \hline
        \texttt{isActive} & Boolean & Default: true & Trạng thái kinh doanh của sản phẩm. \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Cấu trúc collection \texttt{orders} (Đơn hàng)}
    \label{table:schema_order}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.0cm}|>{\raggedright\arraybackslash}p{1.7cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{5.3cm}|}
        \hline
        \textbf{Trường dữ liệu} & \textbf{Kiểu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\ \hline
        \texttt{\_id} & ObjectId & Khóa chính & Định danh duy nhất của đơn hàng. \\ \hline
        \texttt{userId} & ObjectId & Ref: user & Người mua hàng. \\ \hline
        \texttt{items} & Array & -- & Danh sách chi tiết các mặt hàng (snapshot tại thời điểm đặt). \\ \hline
        \texttt{pricing} & Object & -- & Bảng giá: subtotal, shopDiscount, platformDiscount, shippingFee, shippingDiscount, finalTotal. \\ \hline
        \texttt{payment}\allowbreak\texttt{Method} & String & Required & Phương thức: COD, Stripe, VNPay. \\ \hline
        \texttt{payment} & Boolean & Default: false & Trạng thái thanh toán thành công. \\ \hline
        {\scriptsize\begin{tabular}[t]{@{}l@{}}\texttt{stripeSessionId}\\\texttt{vnpPaymentUrl}\\\texttt{vnpTxnRef}\end{tabular}} & String & -- & Dữ liệu phiên hoặc mã giao dịch từ cổng thanh toán Stripe/VNPay. \\ \hline
        \texttt{status} & String & Default: 'Order Placed' & Trạng thái vận chuyển của đơn hàng. \\ \hline
        \texttt{idempotency}\allowbreak\texttt{Key} & String & Unique (cặp) & Khóa chống trùng lặp giao dịch đặt hàng. \\ \hline
        \texttt{stock}\allowbreak\texttt{ReservedAt} & Number & -- & Mốc thời gian hệ thống thực hiện giữ kho cho đơn hàng. \\ \hline
        \texttt{reservation}\allowbreak\texttt{ExpiresAt} & Number & -- & Thời gian hết hạn giữ kho. \\ \hline
        \texttt{vendors} & Array & -- & Danh sách chia nhỏ đơn theo từng shop để điều phối. \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Cấu trúc collection \texttt{vouchers} (Mã giảm giá)}
    \label{table:schema_voucher}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.0cm}|>{\raggedright\arraybackslash}p{1.7cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{5.3cm}|}
        \hline
        \textbf{Trường dữ liệu} & \textbf{Kiểu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\ \hline
        \texttt{\_id} & ObjectId & Khóa chính & Định danh duy nhất của voucher. \\ \hline
        \texttt{code} & String & Unique, Required & Mã ký tự để áp dụng (viết hoa). \\ \hline
        \texttt{type} & String & Required & Loại: \texttt{'SHOP'}, \texttt{'PLATFORM'}, \texttt{'SHIPPING'}. \\ \hline
        \texttt{discountType} & String & Required & Kiểu giảm: \texttt{'PERCENT'} hoặc \texttt{'FIXED'}. \\ \hline
        \texttt{discount}\allowbreak\texttt{Value} & Number & Required & Giá trị giảm giá tương ứng. \\ \hline
        \texttt{minOrder}\allowbreak\texttt{Value} & Number & Default: 0 & Giá trị đơn hàng tối thiểu để áp dụng. \\ \hline
        \texttt{vendorId} & ObjectId & Ref: user & Cửa hàng áp dụng (chỉ dùng cho SHOP voucher). \\ \hline
        \texttt{usageLimit} & Number & Default: 0 & Giới hạn số lần sử dụng tối đa. \\ \hline
        \texttt{usedCount} & Number & Default: 0 & Số lần thực tế đã được dùng. \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Cấu trúc collection \texttt{userinteractions} (Tương tác người dùng)}
    \label{table:schema_interaction}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.0cm}|>{\raggedright\arraybackslash}p{1.7cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{5.3cm}|}
        \hline
        \textbf{Trường dữ liệu} & \textbf{Kiểu} & \textbf{Ràng buộc} & \textbf{Mô tả} \\ \hline
        \texttt{userId} & ObjectId & Ref: user & Định danh người dùng tương tác. \\ \hline
        \texttt{productId} & ObjectId & Ref: product & Định danh sản phẩm tương tác. \\ \hline
        \texttt{interactions} & Object & -- & Số lần viewed, clicked, addedToCart, purchased. \\ \hline
        \texttt{interaction}\allowbreak\texttt{Score} & Number & Default: 0 & Điểm số tương tác tổng hợp động. \\ \hline
        \texttt{last}\allowbreak\texttt{Interaction} & Date & Default: Now & Mốc thời gian tương tác gần nhất. \\ \hline
        \texttt{decayFactor} & Number & Default: 1 & Hệ số suy hao thời gian (chu kỳ bán rã 30 ngày). \\ \hline
    \end{tabular}
\end{table}

% ─── SECTION 3: XÂY DỰNG ỨNG DỤNG ──────────────────────────────────────────
\section{Xây dựng ứng dụng}
\label{section:4.3}

\subsection{Thư viện và công cụ sử dụng}
Bảng \ref{table:tools_backend} và Bảng \ref{table:tools_frontend} tổng hợp các công cụ, thư viện và nền tảng chính được sử dụng trong quá trình xây dựng hệ thống. Các phiên bản được lấy trực tiếp từ các tệp \texttt{package.json} của dự án.

\begin{table}[H]
    \centering
    \caption{Thư viện và công cụ chính phía máy chủ}
    \label{table:tools_backend}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{2.5cm}|>{\raggedright\arraybackslash}p{3.7cm}|>{\raggedright\arraybackslash}p{2.0cm}|>{\raggedright\arraybackslash}p{5.6cm}|}
        \hline
        \textbf{Nhóm} & \textbf{Công cụ / thư viện} & \textbf{Phiên bản} & \textbf{Vai trò sử dụng} \\ \hline
        Runtime & Node.js + Express & LTS / 5.1.0 & Xây dựng REST API, middleware và điều phối nghiệp vụ. \\ \hline
        Cơ sở dữ liệu & MongoDB + Mongoose & -- / 8.14.1 & Lưu trữ dữ liệu nghiệp vụ và định nghĩa lược đồ tài liệu. \\ \hline
        Cache / session & Redis & 4.7.1 & Giới hạn tần suất và quản lý phiên xác thực phía server. \\ \hline
        Xác thực & JWT + bcrypt & 9.0.2 / 5.1.1 & Ký token truy cập, refresh token rotation và băm mật khẩu. \\ \hline
        Thanh toán & Stripe & 18.1.0 & Tạo checkout session và xác minh thanh toán qua webhook. \\ \hline
        Ảnh sản phẩm & Cloudflare R2 (AWS SDK S3) + Sharp & 3.965.0 / 0.34.5 & Lưu trữ ảnh sản phẩm trên R2 và xử lý, nén ảnh trước khi lưu. \\ \hline
        Realtime & Socket.IO & 4.8.3 & Chat hai chiều giữa buyer và vendor. \\ \hline
        Kiểm thử & Jest + mongodb-memory-server & 30.3.0 / 11.0.1 & Kiểm thử đơn vị cho các service nghiệp vụ. \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Thư viện và công cụ chính phía giao diện và triển khai}
    \label{table:tools_frontend}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{2.5cm}|>{\raggedright\arraybackslash}p{3.7cm}|>{\raggedright\arraybackslash}p{2.0cm}|>{\raggedright\arraybackslash}p{5.6cm}|}
        \hline
        \textbf{Nhóm} & \textbf{Công cụ / thư viện} & \textbf{Phiên bản} & \textbf{Vai trò sử dụng} \\ \hline
        UI framework & React + Vite & 19.0.0 / 6.2.0 & Xây dựng SPA cho khách hàng cuối. \\ \hline
        Điều hướng & React Router DOM & 7.5.0 & Quản lý route phía client. \\ \hline
        HTTP client & Axios & 1.9.0 & Gọi REST API với interceptor tự động làm mới token. \\ \hline
        WebSocket client & socket.io-client & 4.8.3 & Kết nối kênh chat thời gian thực từ phía client. \\ \hline
        CSS utility & Tailwind CSS & 3.4.17 & Thiết kế giao diện responsive theo lớp tiện ích. \\ \hline
        Thông báo UI & React Toastify & 11.0.5 & Hiển thị toast notification. \\ \hline
        Đăng nhập Google & @react-oauth/google & 0.13.4 & Tích hợp đăng nhập Google OAuth 2.0. \\ \hline
        IDE & Visual Studio Code & 1.90+ & Soạn thảo mã nguồn chính. \\ \hline
        Quản lý phiên bản & Git / GitHub & 2.45+ & Kiểm soát phiên bản và hợp nhất nhánh. \\ \hline
        Kiểm thử API & Postman & 11+ & Thử nghiệm endpoint và tài liệu REST API. \\ \hline
        Tunnel phát triển & Cloudflare Tunnel & -- & Phơi máy chủ cục bộ để kiểm thử webhook thanh toán. \\ \hline
    \end{tabular}
\end{table}

\subsection{Kết quả đạt được}
Kết quả đầu ra là một hệ thống hoàn chỉnh gồm hai ứng dụng giao diện và một backend API tích hợp thanh toán, realtime, lưu trữ ảnh và xác thực. Hệ thống hiện thực đầy đủ các nhóm nghiệp vụ cốt lõi đã xác lập từ Chương 2: xác thực hợp nhất user/vendor, tìm kiếm server-side, gợi ý sản phẩm lai, giỏ hàng, preview hóa đơn, checkout đa nhà bán hàng, thanh toán COD/Stripe/VNPay, quản trị shop, chat và thông báo.

Bảng \ref{table:stats_codebase} trình bày một số chỉ số định lượng của hệ thống.

\begin{table}[H]
    \centering
    \caption{Thống kê quy mô mã nguồn và thành phần triển khai}
    \label{table:stats_codebase}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{3.5cm}|>{\raggedright\arraybackslash}p{2.6cm}|>{\raggedright\arraybackslash}p{2.5cm}|>{\raggedright\arraybackslash}p{4.9cm}|}
        \hline
        \textbf{Thành phần} & \textbf{Số tệp mã nguồn} & \textbf{Số dòng mã} & \textbf{Ghi chú} \\ \hline
        Backend API & 109 & $\approx$12.550 & 14 model, 15 route, 16 controller, 26 service nghiệp vụ. \\ \hline
        Frontend shop & 67 & $\approx$11.115 & 23 màn hình/chức năng chính, context, hooks, components. \\ \hline
        Vendor dashboard & 20 & $\approx$5.131 & 6 màn hình chính cho quản trị sản phẩm, đơn hàng, voucher, thống kê và chat. \\ \hline
        Toàn hệ thống & 196 & $\approx$28.796 & Chưa tính tài nguyên nhị phân và thư viện cài đặt. \\ \hline
    \end{tabular}
\end{table}

Sau khi build production, bản \texttt{frontend/dist} có dung lượng xấp xỉ 9,04 MB và bản \texttt{admin/dist} có dung lượng khoảng 0,85 MB. Cả hai ứng dụng giao diện đều có thể đóng gói vào image Docker và phục vụ qua Nginx, trong khi backend được triển khai riêng trên cổng \texttt{4000}.

\subsection{Minh họa các chức năng chính}

\textbf{Trang chủ và tìm kiếm sản phẩm.} Trang chủ hiển thị banner hero, danh sách sản phẩm bán chạy và thanh tìm kiếm với gợi ý tức thì. Kết quả tìm kiếm hỗ trợ lọc theo danh mục, khoảng giá và sắp xếp theo mức độ phù hợp, số lượng bán hoặc giá (Hình \ref{fig:ss_home}).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.78\textheight,keepaspectratio]{Chuong/images/generated/ss_home.png}
    \caption{Giao diện trang chủ và tìm kiếm sản phẩm}
    \label{fig:ss_home}
\end{figure}

\textbf{Trang chi tiết sản phẩm.} Người dùng có thể chọn thuộc tính SKU (màu sắc, kích thước) trực tiếp trên trang; các tùy chọn dẫn đến tổ hợp hết hàng được gạch chéo và vô hiệu hóa tự động dựa trên tồn kho thực tế của từng SKU. Trang còn hiển thị đánh giá người dùng, sản phẩm liên quan và nút trò chuyện với nhà bán hàng (Hình \ref{fig:ss_product}).

\begin{figure}[H]
    \centering

     \includegraphics[width=0.95\textwidth,height=0.78\textheight,keepaspectratio]{Chuong/images/generated/ss_product.png}

    \caption{Giao diện chi tiết sản phẩm với chọn SKU và trạng thái tồn kho}
    \label{fig:ss_product}
\end{figure}

\textbf{Quy trình thanh toán.} Trang đặt hàng hiển thị xem trước bảng giá nhiều lớp gồm giá gốc, chiết khấu từng loại voucher và tổng cuối được tính theo thời gian thực khi người dùng nhập mã. Hệ thống từ chối rõ ràng các voucher không hợp lệ kèm lý do (Hình \ref{fig:ss_checkout}).

\begin{figure}[H]
    \centering

        \includegraphics[width=0.95\textwidth,height=0.78\textheight,keepaspectratio]{Chuong/images/generated/ss_voucher.png}

    \caption{Giao diện đặt hàng và bảng giá nhiều lớp voucher}
    \label{fig:ss_checkout}
\end{figure}

\textbf{Bảng điều khiển nhà bán hàng.} Vendor quản lý sản phẩm, theo dõi trạng thái đơn hàng và xác nhận giao hàng qua giao diện riêng biệt. Màn hình tổng quan dashboard được trình bày trong Hình \ref{fig:ss_vendor_overview}; giao diện quản lý sản phẩm, đơn hàng và voucher lần lượt được minh họa trong Hình \ref{fig:ss_vendor_product}, Hình \ref{fig:ss_vendor_order} và Hình \ref{fig:ss_vendor_voucher}. Bên cạnh đó, Hình \ref{fig:ss_vendor_chat} thể hiện khu vực chat và thông báo thời gian thực, nơi vendor nhận cập nhật mới qua SSE và trao đổi trực tiếp với khách hàng qua Socket.IO.

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/generated/vendor_dashboard_overview.png}{
        \includegraphics[width=0.92\textwidth,height=0.34\textheight,keepaspectratio]{Chuong/images/generated/vendor_dashboard_overview.png}
    }{
        \fbox{\rule{0pt}{6.2cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Màn hình tổng quan của bảng điều khiển nhà bán hàng}
    \label{fig:ss_vendor_overview}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/generated/vendor_product_management.png}{
        \includegraphics[width=0.92\textwidth,height=0.34\textheight,keepaspectratio]{Chuong/images/generated/vendor_product_management.png}
    }{
        \fbox{\rule{0pt}{6.2cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Màn hình quản lý sản phẩm của nhà bán hàng}
    \label{fig:ss_vendor_product}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/generated/vendor_order_management.png}{
        \includegraphics[width=0.92\textwidth,height=0.34\textheight,keepaspectratio]{Chuong/images/generated/vendor_order_management.png}
    }{
        \fbox{\rule{0pt}{6.2cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Màn hình quản lý đơn hàng của nhà bán hàng}
    \label{fig:ss_vendor_order}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/generated/vendor_voucher_management.png}{
        \includegraphics[width=0.92\textwidth,height=0.34\textheight,keepaspectratio]{Chuong/images/generated/vendor_voucher_management.png}
    }{
        \fbox{\rule{0pt}{6.2cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Màn hình quản lý voucher của nhà bán hàng}
    \label{fig:ss_vendor_voucher}
\end{figure}

\begin{figure}[H]
    \centering
    \IfFileExists{Chuong/images/generated/vendor_chat_notifications.png}{
        \includegraphics[width=0.92\textwidth,height=0.34\textheight,keepaspectratio]{Chuong/images/generated/vendor_chat_notifications.png}
    }{
        \fbox{\rule{0pt}{6.2cm}\rule{0.90\textwidth}{0pt}}
    }
    \caption{Màn hình chat và thông báo thời gian thực của nhà bán hàng}
    \label{fig:ss_vendor_chat}
\end{figure}
% ─── SECTION 4: KIỂM THỬ ────────────────────────────────────────────────────
\section{Kiểm thử}
\label{section:4.4}

\textbf{Kỹ thuật kiểm thử.} Đồ án áp dụng hai kỹ thuật kiểm thử bổ sung cho nhau: kiểm thử hộp trắng (white-box) ở cấp đơn vị cho lớp Service và kiểm thử thủ công (exploratory) ở cấp hệ thống cho luồng người dùng đầu cuối. Framework kiểm thử sử dụng Jest 30.3 với cú pháp ES Module gốc; \texttt{mongodb-memory-server} cung cấp MongoDB trong bộ nhớ để chạy kiểm thử không phụ thuộc cơ sở dữ liệu thực.

\textbf{Kiểm thử đơn vị -- Dịch vụ Voucher.} Module \texttt{voucherService.js} chứa logic tính giá nhiều lớp và được kiểm thử kỹ nhất. Bảng \ref{table:test_voucher} liệt kê 11 trường hợp kiểm thử thuộc ba nhóm.

\begin{table}[H]
\centering
\caption{Các trường hợp kiểm thử module VoucherService (11/11 đạt)}
\label{table:test_voucher}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|>{\centering\arraybackslash}p{0.6cm}|>{\raggedright\arraybackslash}p{4.7cm}|>{\raggedright\arraybackslash}p{4.2cm}|>{\raggedright\arraybackslash}p{3.6cm}|}
    \hline
    \textbf{TC} & \textbf{Mô tả} & \textbf{Đầu vào chính} & \textbf{Kết quả mong đợi} \\ \hline
    \multicolumn{4}{|l|}{\textit{calcVoucherDiscount}} \\ \hline
    1 & Giảm phần trăm với giới hạn tối đa & 500.000đ, PERCENT 20\%, max 50.000đ & Giảm 50.000đ \\ \hline
    2 & Giảm cố định không vượt giá trị đơn & 10.000đ, FIXED 15.000đ & Giảm 10.000đ \\ \hline
    3 & Không giảm khi đơn chưa đạt tối thiểu & 90.000đ, min 100.000đ & Giảm 0đ \\ \hline
    \multicolumn{4}{|l|}{\textit{computeOrderPricing}} \\ \hline
    4 & Kịch bản mẫu 3 voucher, tổng 400.000đ & SHOP 15\% + PLATFORM 20k + FREESHIP 25k & Cuối 355.000đ \\ \hline
    5 & Tổng không âm khi giảm vượt giá trị & Platform và ship đều giảm 1.000.000đ & Cuối 0đ \\ \hline
    6 & Từ chối voucher shop sai nhà bán & Voucher shop-b nhưng đơn chỉ có shop-a & Từ chối, shopDiscount = 0 \\ \hline
    7 & Giới hạn 1 voucher shop mỗi nhà bán & 2 voucher SHOP cùng shop-a & Áp dụng 1, từ chối 1 \\ \hline
    8 & Giới hạn 1 voucher nền tảng mỗi đơn & 2 voucher PLATFORM cùng đơn & Áp dụng 1, từ chối 1 \\ \hline
    9 & Giới hạn 1 voucher vận chuyển mỗi đơn & 2 voucher SHIPPING cùng đơn & Áp dụng 1, từ chối 1 \\ \hline
    \multicolumn{4}{|l|}{\textit{assertVoucherValid}} \\ \hline
    10 & Từ chối voucher hết hạn & endAt ở quá khứ & Ném ngoại lệ \\ \hline
    11 & Từ chối voucher hết lượt & usedCount = usageLimit & Ném ngoại lệ \\ \hline
\end{tabular}
\end{table}

Kết quả: 11/11 trường hợp kiểm thử của module \texttt{VoucherService} đạt. Ngoài bộ kiểm thử này, dự án còn duy trì thêm bộ kiểm thử cho \texttt{interactionService} nhằm kiểm tra logic ghi nhận tương tác và gợi ý sản phẩm, cùng bộ kiểm thử \texttt{stockReservationService} để xác nhận giữ kho cạnh tranh, rollback tồn kho khi một sản phẩm hết hàng và chỉ mục chống trùng đơn. Tính đến thời điểm rà soát, toàn bộ 22/22 ca kiểm thử tự động của backend đều đạt.

\textbf{Kiểm thử thủ công luồng đặt hàng.} Bảng \ref{table:manual_test} ghi nhận kết quả kiểm thử thủ công đối với bốn kịch bản trọng tâm.

\begin{table}[H]
    \centering
    \caption{Kết quả kiểm thử thủ công luồng đặt hàng và giữ kho nguyên tử}
    \label{table:manual_test}
    \small
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{4.0cm}|>{\centering\arraybackslash}p{1.4cm}|>{\raggedright\arraybackslash}p{7.2cm}|}
        \hline
        \textbf{Kịch bản} & \textbf{Kết quả} & \textbf{Quan sát} \\ \hline
        Hai request đồng thời đặt cùng sản phẩm, tồn kho bằng 1 & Đạt & Chỉ một đơn thành công; đơn còn lại nhận lỗi không đủ tồn kho; tồn kho sau kiểm tra là 0, không âm. \\ \hline
        Gửi lại cùng request với mã định danh chống trùng đơn cũ & Đạt & Backend trả về kết quả đơn hàng đã tạo trước đó, không phát sinh đơn trùng. \\ \hline
        Thanh toán online để quá hạn thời hạn giữ kho & Đạt & Tác vụ nền hoàn trả tồn kho, khôi phục lượt dùng voucher và chuyển trạng thái đơn sang đã hủy. \\ \hline
        Đặt hàng với voucher shop ngoài phạm vi & Đạt & Backend bỏ qua voucher ngoài phạm vi, tổng tiền vẫn tính đúng cho các voucher hợp lệ còn lại. \\ \hline
    \end{tabular}
\end{table}

\textbf{Kiểm thử hiệu năng và tranh chấp đồng thời dưới tải cao.} Để kiểm chứng giới hạn chịu tải và độ tin cậy của cơ chế giữ kho nguyên tử trong tình huống tải cao (giả lập sự kiện Flash Sale), đồ án đã xây dựng và thực thi kịch bản kiểm thử tranh chấp đồng thời thông qua công cụ mô phỏng tải. Thử nghiệm được tiến hành với hai cấp độ tải khác nhau: tải trung bình mô phỏng 100 yêu cầu đồng thời (VUs) cạnh tranh trên 20 đơn vị tồn kho, và tải cao mô phỏng 250 yêu cầu đồng thời cạnh tranh trên 50 đơn vị tồn kho. Toàn bộ các yêu cầu được kích hoạt gửi đi đồng loạt tại cùng một thời điểm.

Kết quả đo lường và thống kê hiệu năng chi tiết được trình bày trong Bảng \ref{table:load_test_results}.

\begin{table}[H]
    \centering
    \caption{Kết quả kiểm thử hiệu năng tranh chấp giữ kho dưới tải cao}
    \label{table:load_test_results}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{|>{\raggedright\arraybackslash}p{5.4cm}|>{\centering\arraybackslash}p{2.9cm}|>{\centering\arraybackslash}p{2.9cm}|}
        \hline
        \textbf{Chỉ số hiệu năng} & \textbf{Tải trung bình (100 VUs)} & \textbf{Tải cao (250 VUs)} \\ \hline
        Số lượng yêu cầu đồng thời & 100 & 250 \\ \hline
        Tồn kho sản phẩm ban đầu & 20 & 50 \\ \hline
        Số đơn hàng thành công & 20 (100\% tồn kho) & 50 (100\% tồn kho) \\ \hline
        Số yêu cầu bị từ chối (Hết hàng) & 80 & 200 \\ \hline
        Tỉ lệ bán vượt (Oversell Rate) & \textbf{0.00\%} & \textbf{0.00\%} \\ \hline
        Tổng thời gian thực thi & 401.14 ms & 679.55 ms \\ \hline
        Thời gian phản hồi trung bình (Average) & 321.54 ms & 523.79 ms \\ \hline
        Thời gian phản hồi nhanh nhất (Min) & 281.26 ms & 463.07 ms \\ \hline
        Thời gian phản hồi chậm nhất (Max) & 398.66 ms & 677.49 ms \\ \hline
        Băng thông trung bình (Throughput) & \textbf{249.29 reqs/s} & \textbf{367.89 reqs/s} \\ \hline
        Tồn kho thực tế cuối cùng trong DB & 0 & 0 \\ \hline
    \end{tabular}
\end{table}

Phân tích kết quả thực nghiệm cho thấy:
\begin{itemize}
    \item \textbf{Độ chính xác tồn kho tuyệt đối}: Ở cả hai cấp độ tải, hệ thống đều xử lý thành công đúng số lượng đơn hàng tương ứng với lượng tồn kho thực tế (20 đơn cho tải 100 và 50 đơn cho tải 250). Toàn bộ các yêu cầu còn lại đều bị chặn và phản hồi mã lỗi \texttt{OUT\_OF\_STOCK}. Số lượng tồn kho cuối cùng được ghi nhận trong cơ sở dữ liệu về đúng mức 0, tỉ lệ bán vượt (oversell) đạt mức $0.00\%$.
    \item \textbf{Khả năng mở rộng hiệu năng (Throughput)}: Khi tăng tải gấp 2.5 lần (từ 100 lên 250 request), băng thông xử lý của hệ thống tăng từ 249.29 reqs/s lên 367.89 reqs/s. Điều này chứng minh vòng lặp sự kiện (Event Loop) của Node.js xử lý các luồng I/O bất đồng bộ cực kỳ tối ưu, giúp tối đa hóa năng suất phục vụ của CPU.
    \item \textbf{Thời gian phản hồi (Latency) ổn định}: Dưới áp lực tải cao của 250 request gửi cùng lúc, thời gian phản hồi trung bình chỉ là 523.79 ms và request chậm nhất hoàn tất trong 677.49 ms. Khoảng chênh lệch thời gian phản hồi thấp chứng tỏ không xảy ra hiện tượng nghẽn luồng xử lý hoặc chờ đợi khóa kéo dài.
    \item \textbf{Tối ưu hóa tài nguyên cơ sở dữ liệu}: Do sử dụng cơ chế cập nhật ghi nguyên tử trực tiếp (\texttt{Atomic Write} bằng phép toán cập nhật kết hợp điều kiện sàng lọc trực tiếp trên MongoDB) thay vì khởi tạo các giao dịch (Transactions) đa bước phức tạp hay sử dụng khóa bi quan (Pessimistic Locking), cơ sở dữ liệu đã giải phóng hàng đợi truy vấn ngay lập tức, tránh gây nghẽn kết nối (connection pool exhaustion).
\end{itemize}

% ─── SECTION 5: TRIỂN KHAI ──────────────────────────────────────────────────
\section{Triển khai}
\label{section:4.5}

Hệ thống được tổ chức theo hai mô hình triển khai. Ở môi trường phát triển cục bộ, ba tiến trình được khởi động riêng biệt: backend tại cổng \texttt{4000}, frontend shop tại cổng \texttt{5173} và vendor dashboard tại cổng \texttt{5174} (Vite dev server). Cách triển khai này phù hợp cho giai đoạn phát triển và kiểm thử từng thành phần độc lập. Cloudflare Tunnel được dùng để phơi cổng 4000 ra Internet tạm thời, phục vụ nhận webhook từ Stripe trong khi kiểm thử thanh toán cục bộ.

Đối với môi trường container hóa, dự án cung cấp cấu hình Docker Compose cho cả dev và production. Bản production build cả frontend shop lẫn vendor dashboard thành tài nguyên tĩnh rồi phục vụ qua Nginx, còn backend chạy trong container Node.js riêng. Nhờ vậy, các thành phần chính vẫn tách biệt nhưng có thể được triển khai đồng bộ bằng một lệnh duy nhất.

MongoDB được cấu hình theo hướng linh hoạt thông qua biến môi trường \texttt{MONGODB\_URI}, do đó có thể kết nối MongoDB Atlas (khu vực Singapore, tier M0) hoặc máy chủ nội bộ mà không cần thay đổi mã nguồn. Redis được backend sử dụng khi biến \texttt{REDIS\_URL} khả dụng; nếu không kết nối được, hệ thống có cơ chế fallback trong bộ nhớ cho xác thực và giới hạn tần suất, song đây chỉ là phương án dự phòng cho phát triển chứ không phải cấu hình production khuyến nghị.

Về tích hợp cổng thanh toán, Stripe webhook được gắn tại endpoint \path{/api/order/stripe-webhook}. Endpoint này phải được mount trước middleware \path{express.json()} để giữ nguyên raw body phục vụ xác minh chữ ký. Với VNPay, backend xác minh chữ ký của tham số callback rồi redirect người dùng về trang \path{/verify} ở frontend. Cách tổ chức này giữ cho luồng thanh toán online nhất quán giữa backend, frontend và nhà cung cấp dịch vụ thanh toán.

\section*{Kết chương}
Chương 4 đã hiện thực hóa các định hướng công nghệ của Chương 3 thành một cấu trúc hệ thống cụ thể gồm kiến trúc nhiều tầng, các mô-đun nghiệp vụ tách lớp, mô hình dữ liệu cho marketplace đa nhà bán hàng, ứng dụng giao diện và backend tích hợp thanh toán, realtime và khuyến nghị. Chương cũng đã tổng hợp công cụ sử dụng, thống kê quy mô mã nguồn, kết quả 22/22 ca kiểm thử tự động ở các module \texttt{VoucherService}, \texttt{interactionService} và \texttt{stockReservationService}, thực nghiệm đánh giá hiệu năng tranh chấp đồng thời dưới tải cao, cùng bốn kịch bản kiểm thử thủ công đầu cuối, đồng thời mô tả rõ cách thức triển khai trong môi trường local và Docker. Những kết quả này là cơ sở trực tiếp để chuyển sang Chương 5, nơi các giải pháp và đóng góp nổi bật sẽ được phân tích dưới góc nhìn kỹ thuật và giá trị thực tiễn.

\end{document}







